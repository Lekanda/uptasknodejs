<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en UpTask

            form#formulaio.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Introduce tu Email")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Introduce tu Password")
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta")
                .campo
                    a(href="/iniciar-sesion") Volver a Iniciar Sesion
        script(src="/dist/bundle.js")
        
