extends layout

include mixins/crearProyecto

block contenido
    .contenido-principal
        h1 #{nombrePagina}

        //- pre= vardump(proyectos)

        if errores
            each error in errores
                .alerta.error= error.texto
        +crearProyecto()
            
        