<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Iniciar Sesion en UpTask

            if error 
                p.alerta.error= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="text" name="email" placeholder="Introduce tu Email" value= email)
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="Introduce tu Password" value= password)
                .campo.enviar
                    input.boton(type="submit" value="Iniciar Sesion")
                .campo
                    a(href="/crear-cuenta") ¿Aun no tienes Cuenta? Crea una gratis